'use strict'

/* 
1. Переменной «a» присваивается произведение переменной «a» и цифры 2 (a=2*2=4);
2. К цифре 1 прибавляется значение полученное в скобках (1+4=5);
3. Переменной «x» присваивается полученное значение (x=5). 
*/
let a = 2;
let x = 1 + (a *= 2); // x==5, a==4